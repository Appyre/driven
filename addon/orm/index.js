const primitives = require('./schema-primitives');
const relationships = require('./relationships');
const types = require('./types');
const Model = require('./model');
const Schema = require('./schema');
const Table = require('./table');

module.exports = {
    primitives,
    relationships,
    types,
    Model,
    Schema,
    Table
};